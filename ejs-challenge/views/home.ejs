<%- include('partials/header') %>


  <h1>HOME</h1>
  <article><%= homeStartingContent %></article>

  <% posts.forEach(function(post){ %>
    <div>
      <h2><%= post.title %></h2>
      <p><%= post.content %></p>
    </div>
  <% }) %>

<%- include('partials/footer') %>